---
description: Start the Jekyll development server
---

Start the local Jekyll development server on port 4000. This should automatically move to running in the background as soon as it outputs 'Server running...'

```bash
bundle install
bundle exec jekyll serve
```